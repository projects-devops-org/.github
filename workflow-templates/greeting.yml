name: Template about greeting
on: 
  push:
    branches: [$default-branch]
jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello, ${{ github.actor }}! Welcome to the ${{ github.repository }} repository."